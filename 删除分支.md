# 1、删除 GitHub 网页上的 dev（远程分支 origin/dev）
	1. 仓库页面点 Branches
	2. 找到 dev，点右边垃圾桶图标删除
# 2、删除你电脑本地的 dev 分支（Git‑GUI 操作）
	1. 回到 Git‑GUI，现在你在 master 分支
	2. 顶部菜单 `Branch` → `Delete…`
	3. 选中本地的 `dev`，点 Delete，删掉本机 dev 分支。
	4. 再执行一次 `Remote` → `Fetch from origin`，这一步会自动清理掉失效的 `origin/dev` 远程追踪记录。

> 实操顺序总结：

1. GitHub 网页删掉远程 dev
2. Git‑GUI 切 master，删除本地 dev 分支
3. 可选：清理掉本地的 origin/dev 追踪记录。